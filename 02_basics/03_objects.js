// singleton
// created with constructor

// object literal

const mySymbol = Symbol("key1")

const user = {
    name: "Amardeep",
    age: 18,
    'full name': "Amardeep Phule",

}

console.log(user.name);
console.log(user['age']);
console.log(user['full name']);